require 'spec_helper'

describe MlbScores do
  it 'has a version number' do
    expect(MlbScores::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
